export interface Project {
    id: number;
    name: string;
    comment_count: number;
    color: number;
    shared: boolean;
    sync_id: number;
    order: number;
    favorite: boolean;
    url: string;
}